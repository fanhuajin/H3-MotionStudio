# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Product decisions

- 人物定妆功能当前保留实现与素材，但从侧边栏隐藏并关闭 `/portrait` 前端路由；API 暂不删除，便于以后恢复。

- 歌词字幕功能（2026-09-10 用户反馈「效果太差」）同样从侧边栏隐藏并关闭 `/lyrics` 前端路由：`src/App.tsx` 移除入口与路由分支、后端删除 `/lyrics` 的 SPA 回退（直接访问返回 404），但 `src/LyricRoute.tsx` 与 `/api/lyrics/*`、`POST /api/jobs/lyrics`、`pipeline.run_lyrics_job` 全部保留，便于以后恢复；**批量制作里的「生成歌词字幕版」步骤也已在 2026-09-14 彻底去掉**（见下方批量交付条目）。

- 批量制作是 `/batch` 路由：默认在页面内分别向“歌曲视频”和“跳舞视频”多行输入区粘贴任意数量的抖音链接，表格导入/导出只作为可选的批次备份方式、不是启动前提。页面可直接启动、暂停、跳过、重试或删除单条，并从持久化状态同步当前条目及真实子任务进度；批次严格逐条运行并复用全局 `pipeline_lock`，不要求先从 Codex 启动。每条先自动下载、分析并生成适合 ComfyUI 稳定出片的候选人物图，同时生成一套标题/简介/标签，然后停在该条目的页面审核点；用户确认某一条后该条视频立即开始，不必等待整批都确认。审核区只做「看图 + 确认」，**不放任何教学式提示或修改回路**（2026-09-10 用户逐项要求删除：出图素材面板、下载/复制按钮、「哪里不合适」反馈框与「只调图片/只调文案/两者都调整」、`style_note` 的「已沿用源视频造型」徽章、`cover_headline` 封面标题字段）。用户直接在 ChatGPT 网页里迭代图片与文案，不需要系统再提供调整入口；后端 `request_review_adjustment` 与 `mode` 逻辑保留但前端不再暴露。封面大字直接用 `title`（`render_covers` 以 title 为唯一来源，不再有独立的封面标题字段）。出图素材（提示词 + 图一 + 图二）不通过页面按钮交付，提示词落盘在 `data/batches/{batch}/{item}/出图提示词.txt`，图一为同目录 `scene-frame.jpg`，图二为 `E:\AI_Assets\PortraitIdentity\本人固定参考.png`。只有用户点击“确认并继续生成视频”才放行，允许在审核点前启动 ComfyUI，但**仅限生成候选人物图**；确认之前一律不得提交任何视频生成、二采、RVC 与歌词字幕任务。删除运行中的单条必须先安全取消其子任务。页面链接草稿、审核意见和后端批次状态必须持久化，关闭、刷新或下次进入后自动恢复。
- 批量预审不得再驱动 Codex CLI：`codex exec` 每条都要起一个完整 agent 会话，吃 ChatGPT/Codex 订阅额度、单条十几分钟且会挂死（2026-09-10 实测卡在 92% 十余小时）。预审 = 本地 ffmpeg 一次抽取覆盖全时段的 6 帧联系表（禁止重复扫描完整视频）→ 直连 `gpt-5.6-luna` 一次调用产出造型来源判断、标题/简介/标签，以及动作/运镜时间轴（跳舞则为迁移提示词，动作/运镜返回空）→ 出候选人物图。模型与出图都必须可降级：模型失败退回源作品文案、出图失败退回源视频取帧，单条降级只标黄不阻断整批；只有视频/二采/RVC 失败才停下来等人工处理。预审进度只在有真实采样步数时显示百分比，出图耗时较长时必须同时显示已耗时。造型提示词收在 `backend/prompts/`（`H3_BATCH_*_PROMPT` 可覆盖），不再依赖桌面绝对路径。
- 候选人物图的出图来源由 `H3_BATCH_IMAGE_PROVIDER` 决定：**默认 `manual`** —— 用户明确要求「去掉图片生成，图片由我自己去 gpt 聊天补充」，系统只备料（提示词 + 图一 + 图二）并接收用户上传的成图。其它可选值：`api` 走中转站、`local` 走本地 ComfyUI Krea2 双图编辑（图像-1 源视频取帧、图像-2 固定身份图，`4:3` 唱歌 / `9:16` 跳舞）、`frame` 直接用源视频取帧、`auto`（配了中转站才走 api，否则 manual）。**默认不要用 `local`**：8GB 卡上 `krea2_turbo_int8` 每步要经 PCIe 搬权重，10 步采样就 63.6 秒（Krea2 出图总计 74.6 秒），且画质被用户明确否决（左右眼不对称、皮肤塑料感、发丝发糊）。官方 API 的 `gpt-image-*` 全报 `credit_balance_exhausted`，所以出图走用户自备的国内中转站，配置项是 `H3_BATCH_IMAGE_BASE_URL` / `H3_BATCH_IMAGE_API_KEY` / `H3_BATCH_IMAGE_MODEL`（不走 `OPENAI_BASE_URL`）；`configured()` 要求显式配了中转站 base_url，避免默认把请求打到没有余额的官方图片接口。文本分析固定走官方端点，用 `H3_BATCH_TEXT_BASE_URL` / `H3_BATCH_TEXT_API_KEY` 覆盖，故意不读 `OPENAI_BASE_URL`（中转站没有 luna）。凭据只从环境读取（`settings.env_value` 支持回读 Windows 用户级环境变量），严禁写入前端、数据库或仓库。
- 中转站（TeamoRouter 一类）请求细节，改之前先看清：`POST {base}/images/edits` 走 multipart、`Authorization: Bearer <key>`、结果取 `data[0].b64_json`。官方多图字段是 `image[]`，但中转站文档常只写单数 `image`，所以 `batch_image.request_plans()` 按「`image[]` → `image` → 拼合成单图」依次回退；`H3_BATCH_IMAGE_FIELD` 可钉死字段名，`H3_BATCH_IMAGE_MODE=composite` 可强制走拼合（拼合图不叠文字，只用提示词说明左右半幅各自角色，避免标注被画进成图）。尺寸必须用精确比例 `1536x1152`（真 4:3）与 `1152x2048`（真 9:16），**不要用 1536x1024** —— 那是 3:2；中转站要求宽高均为 16 的倍数、长宽比 ≤3:1、总像素 655,360~8,294,400。编辑接口带 `input_fidelity=high`（身份保持）与 `quality=high`；该站按出图**实际分辨率**分 1K/2K/4K 三档计费，质量参数不额外计价。文本分析也可以改走中转站（`GPT-5.6 Luna` 在该站约 1 折），把 `H3_BATCH_TEXT_BASE_URL` / `H3_BATCH_TEXT_API_KEY` 指过去即可，不必依赖官方那份计划内额度。
- 批量出图与文案的硬性规则（2026-09-10 用户逐条确认）：**发布标签固定 5 个**（schema `minItems`/`maxItems` 都是 5）。**五官必须与原型图 `E:\AI_Assets\PortraitIdentity\本人固定参考.png` 完全一致**，这是最高优先级：提示词末尾固定附加 `batch_ai.IDENTITY_PRIORITY_BLOCK`（模型对末尾最敏感），并且出图前必须用 `batch_image.defocus_reference_face()` 把参考帧（图像-1）里的人脸高斯模糊掉 —— 图像-1 里是源视频那个人的脸，不屏蔽模型就会把别人的五官混进来。**文案必须看着最终候选图写**：`batch_ai.write_copy()` 用用户提供的候选图作为唯一事实来源重写标题/简介/标签（预审只看得到源视频联系表、看不到之后的出图，实测会出现文案写「粉色氛围」而成图是黑发水晶场景的图文脱节）。构图规格写在 `settings.PORTRAIT_COMPOSITION`，数值来自用户两张构图参考图（`ca8aec93-…png` 4:3、`f0a6e57c-…png` 9:16）的实测：4:3 为头顶留白 0%~1%、发际线距上沿约 7%、脸占画面 50%~55%；9:16 为留白约 1%、发际线约 12%、脸占 34%。**出片可用性**约束写在 `batch_ai.VIDEO_READY_BLOCK`：背景简洁有纵深、不要碎钻闪光/散景光点/密集装饰，轮廓稳定、手不入镜或垂放、不得遮挡面部。**进度不得造假**：中转站没有节点级进度时把 `progress` 置为 `None`，前端显示「进行中」脉冲动画 + 已耗时（编号冻在同一个百分比上会被误认为卡死）；只有本地 Krea2 的真实采样步数才显示百分比。
- 批量交付内容（2026-09-14 用户确认后定型）：**只有「最终成片 + 人物图 + 发布文案」三件**，落在 `E:\AI_Exports\H3-MotionStudio\发布成品\{编号}_{歌名}_{作品号}\`：`最终成片.mp4`（`_deliver()` 从子任务的 `finalOutput` 拷入；唱歌与跳舞同一个名字，不再分无字幕/有字幕）、`人物图.png`（**用户上传的那张** `ai.reference_image_path`，用户要求「我上传的图片也要放到指定目录下」）、`发布文案.txt`。`/output` 接口新增 `image` 键；`videoNoLyrics`/`videoWithLyrics` 只是 2026-09-14 之前旧交付留下的键，历史条目仍能点开。
- **批量流程已彻底去掉「生成歌词字幕版」这一步**（2026-09-14 用户：「已经没有生成歌词字幕版，可是流程还是存在」）：`batch_worker.item_milestones()` 不再给歌曲条目加 lyrics 行，`_process_confirmed` 不再提交 `POST /api/jobs/lyrics`；`batch_store.RETIRED_MILESTONE_IDS` 在**读取时**剔掉历史条目里残留的 lyrics 里程碑（读写都过 `_normalize`，所以老条目也会被就地纠正）。`/api/jobs/lyrics`、`pipeline.run_lyrics_job`、`LyricRoute.tsx` 依旧保留以便恢复（见上一条路由说明）。
- **进度只剩四格：下载 / 备料 / 审核 / 出片**（2026-09-13 用户看到「发布文件已整理」后确认「直接去掉这一格」）：`item_milestones()` 删掉 `deliver`（「整理发布文件」）这一格，`RETIRED_MILESTONE_IDS` 加 `deliver`，历史条目读一次就掉；`_mark_delivered()`/`deliver_item_now()`/`_deliver()` 里对 `deliver` 里程碑的写入全部删掉，`reset_review_row()` 与 `app.py` 的重置只认 `video`。**交付本身照做**（照样写 `最终成片.mp4` + 人物图 + `发布文案.txt`），只是不再单独占一格进度；`stage="deliver"` 这个**阶段名**保留（「本条全部信息」里还显示它）。`test_batch_flow_has_four_steps_only` 守住（含历史 `lyrics`/`deliver` 里程碑都被剔除）。
- **跳过 / 删除 / 取消整批绝不能丢掉已经出片的成片**（2026-09-14 用户确认「照样整理」）：2026-09-13 实测用户在 RVC 阶段点「跳过」，子任务 3 秒后照样跑完，但旧实现直接丢弃——发布目录里什么都没有、`视频/歌词字幕/整理发布文件` 全被标成 ✗。现在 `batch_worker._finish_abandoned()` 先调 `_salvage_deliver()`（读 `item.videoJobId` 对应的子任务，`status == completed` 且 `finalOutput` 在磁盘上就照样交付，并把 `video`/`deliver` 里程碑打勾、条目转 `completed`）；那一刻子任务还在跑时，`_spawn_deferred_salvage()` 留一个看护任务等它落定再补一次交付；`POST /api/batches/{id}/cancel` 走 `salvage_abandoned_items()` 同样补交。页面另有「重新整理发布文件」按钮 → `POST /api/batches/{batch}/items/{item}/deliver`（`deliver_item_now()`，幂等），给「成片已生成但没进发布目录」的历史条目用，不用重跑链路。
- **跳过的条目可以重新开始**（2026-09-14 用户：「跳过的视频允许我重新开始」）：`POST /api/batches/{batch}/items/{item}/retry` 从「只接受 failed」放宽到 `{failed, skipped, **completed**}`（`deleted` 仍然拒绝，界面里也已隐藏；`running`/`revising` 必须先「取消出片」或「回到确认」）。重置逻辑按「能不能直接续跑」分档：`reviewApproved` + 有 `ai.reference_image_path` + **`sourcePath` 还在磁盘上** → 回到 `confirmed`，只重跑视频链路（不重复下载/备料）；否则回到 `pending`，从下载与备料重来。**源视频被清理掉时必须回落 pending 重新下载**（2026-09-14 实测：用户清过下载目录，已确认条目直接续跑会在提交时抛「文件不存在」）；`_process_confirmed` 里也补了同样的一道保险，源文件不在就先 `_download()` 再提交。重置同时清掉 `skipRequested`/`deleteRequested`/`error`/`warning`/`childJob`/`videoJobId`/`outputs`，把 `video`/`deliver` 里程碑重置为 `pending`（页面上的流程重新变成待办，而不是停在 ✗），然后 `_resume_batch()` 唤醒 runner；`cancelled` 批次因此也能靠重开单条复活（与「cancelled 不能**追加新链接**」不冲突）。
- **条目操作按钮按状态分工**（2026-09-14 用户：「开始中的任务允许取消重新开始，现在没有取消操作只有删除操作」）：`failed` → 「重试」；`skipped`/`completed` → 「重新开始」；**`confirmed`/`running`/`revising` → 「取消出片」**（红色按钮，走 `skip` 接口：先安全取消子任务，落在 `skipped` 后即可「重新开始」，label 就是用户要的「取消」而不是「跳过」）；`pending`/`awaiting_review` → 「跳过」；任何过了审核点且备过料的条目都有「回到确认」。删除仍然是独立的红色按钮。
- **前端入口不能被缓存**：`backend/app.py` 的五个 SPA 路由（`/batch`、`/migrate`、`/douyin`、`/upscale`、`/rvc`）统一走 `_spa_index()` 并带 `Cache-Control: no-store`。2026-09-14 实测：前端连改三版，用户页面在日志里一直请求最早的 `index-CpyINPSN.js`（新按钮全看不到，只能看到「删除这一条」），根因就是 index.html 被浏览器缓存住、而它指向带 hash 的 bundle。改前端后要让用户**刷新一次**（`test_spa_entry_is_never_cached` 守住这个头）。
- **确认页必须能认出「这是哪条源视频」，并且能就地替换**（2026-09-13 用户：「现在跳舞视频生成的内容不对没有对应上跳舞的视频，你可以在让我确认的时候让我知道现在的是哪个视频吗」+「唱歌视频也让我可以看下你选中的是哪一条视频」+「要有让我可以替换的操作」）：条目上显示的 `title` 是**模型重起的发布标题**（「只对你心动的花季暗号」），用户根本认不出对应哪条抖音视频，所以条目详情顶部固定有一张「本条源视频」卡片（**唱歌与跳舞条目都显示**）：内嵌可播放的源视频（`GET /api/batches/{b}/items/{i}/stage/source`，实测 200/video/mp4）、**源作品自己的文案**（`sourceMetadata.desc` 第一行，即用户在抖音上看到的那句话）、源文件名、**抖音作品号**、「打开抖音原链接」，并标明这条是唱歌还是跳舞条目以及出片时怎么用它。左侧队列每条也带一行「源：〈源作品文案〉」。卡片里给「替换源视频」→ `POST /api/batches/{batch}/items/{item}/source`（body `{url, kind?}` → `batch_worker.replace_item_source()`）：**类型可以一起改**（唱歌视频误贴进跳舞槽位时直接把类型改回来），改了类型就用新类型的默认画布比例；替换会清掉旧源视频与**按它做的**分析（`source-contact-sheet.jpg` / `scene-frame.jpg` / `出图提示词.txt` **必须删文件** —— `_prepare_review_work` 命中缓存即复用，留着会把旧视频的分析套到新视频上）、作废候选图指针/文案/里程碑并排回 `pending` 重新下载备料，**不删用户上传过的图**（只清指针）。`running`/`revising`/`completed` 拒绝（先「取消出片」或「回到确认」），与改比例同一条规则；重复链接（类型 + 链接指纹，自己不算）直接 400。
- **简介 / 标签缺了就自动生成，任何路径都不许留空**（2026-09-13 用户：「流程中简介和标签没有的话自动生成」+「你可以用你的API处理吗，生成这些不需要调用 gpt 的了」；触发场景是官方文本模型 **429 TPM 打满**「Please try again in 9h50m」，`fallback_result` 的 `introduction` 恒为空、`tags` 直接取源作品标签（可能一个都没有），实测条目就是 `intro='' tags=[]`）：① `batch_ai.ensure_copy_fields()` 是**纯本地确定性**兜底（不调模型），把 `introduction` 用歌曲/情绪/源作品文案拼一句，把 `tags` 补齐到**恰好 5 个**（先留着模型或源作品给的，再用 `song_name`、源标签、类型兜底池 `TAG_POOL`（唱歌/跳舞各 5 个）按序补，多了截断）——`PLACEHOLDER_VALUES` 必须剔掉「未识别/未知/无」这类占位值（补标签时会把 `song_name` 也算进候选，实测把「未识别」当标签发出去了）；② `_prepare_review_work` 在写 `ai` 之前调用它，`POST /items/{i}/image`（用户上传图后重写文案）也调用它；③ **`batch_store._backfill_copy_fields()` 放在读取路径的 `_normalize` 里**，所以**已经存在的空文案条目（含历史批次）读一次就自愈**，不必重新备料；模型或 `write_copy` 之后给出真文案时会正常覆盖。想彻底不降级就把文本模型指到已有的中转站（它同样提供 `gpt-5.6-luna`）：`H3_BATCH_TEXT_BASE_URL=https://api.teamorouter.com/v1` + `H3_BATCH_TEXT_API_KEY=<中转站 key>`（**注意 `batch_ai.OPENAI_URL` 是 import 期常量，改完必须重启后端**；官方 `OPENAI_API_KEY` 的 TPM 配额打满后要等约 10 小时）。后端进程**无法调用 agent 自己的模型**，所以无人值守的批量只能靠「本地兜底 + 可配置端点」这两条路。
- **文本/视觉模型可以走 DeepSeek，且它是多模态的**（2026-09-13 用户：「可以用你的生成吗 deepseek」+「deepseek 4.1 不是多模态模型吗 不可以看图吗」）：`deepseek-flash` 实测能看图（把 6 帧联系表发过去能正确描述人物与动作），所以预审造型判断、按成图写文案都能交给它。**唯一的坑是结构化输出**：DeepSeek 对 `response_format: json_schema` 直接 400「This response_format type is unavailable now」，只认 `json_object`，所以 `batch_ai._chat_structured()`（`analyze`/`write_copy`/`locate_face` 三处共用）先按 json_schema 发，被 400 拒绝就记住 `_JSON_SCHEMA_SUPPORTED=False` 并改用 json_object（schema 文字塞进提示词末尾、仍按原 schema 校验必填字段；`H3_BATCH_JSON_MODE=schema|object` 可钉死；返回会剥掉 ```json 代码块）。**配置必须经 `settings.env_value()` 回读 Windows 用户级变量**：后端常由别的进程拉起，`os.getenv` 只有启动时的快照（`LUNA_MODEL`/`OPENAI_URL`/`_api_key()` 三处都补了回退）。当前部署：`H3_BATCH_TEXT_BASE_URL=https://api.deepseek.com/v1`、`H3_BATCH_LUNA_MODEL=deepseek-flash`、`H3_BATCH_TEXT_API_KEY`（Windows 用户级环境变量，**凭据不进仓库/数据库/前端**）。**DSH 自己的 `DEEPSEEK_API_KEY` 不能挪用**（加密在 `~/.dsh/.credentials.yaml`，且 `dsh-subprocess` 不传给子进程）——要用 DeepSeek 必须由用户提供自己的 key。
- **发布文案必须是内容创作者口吻**（2026-09-13 用户：「这个完全不像啊 你这是在陈述啊 我是内容创作者啊」+「简介：🤍 评论区告诉我下一首想看我跳什么～ 不要这种话」）：旧提示词让模型写出「长发女孩身穿酒红色上衣，在蓝色夜景前直视镜头」这种画面说明；后来加了「一句互动号召」又变成每条都以「评论区告诉我…」「你想听我唱哪句？」「你听到第几秒开始跟着晃？」收尾、还带出 `#评论区点歌` 这类标签 —— 两种都不要。现在 `batch_ai.preflight_prompt` 与 `copy_prompt` 都要求：**禁止客观描述句**（不许「画面中/图中/身穿…/站在…前/光线映出」），introduction = 第一人称的情绪或态度（可带 1~2 个 emoji），**不许互动喊话、不许向观众提问**并点名禁止「评论区告诉我 / 你想听我唱哪句 / 点赞关注 / 看到最后别走开」；title ≤20 字带钩子；tags 按创作者用法挑（题材/曲风或舞种/穿搭/氛围/情绪），不许出现「画面」「描述」「评论区」。**图文一致仍要守**（只要求不写画面里没有的元素）。落盘前还有确定性兜底：`sanitize_introduction()` 先截到第一个 `INTERACTION_MARKERS`（评论区/点歌/关注/点赞/双击/收藏/转发/告诉我/你想听/想看我/你会不会/看到最后/你们…）之前、再把结尾那句向观众提问整句删掉（最后一个「你/您」开头、以问号收尾、内部无分句分隔符），剪完不够一句才退回 `compose_introduction()`；`_clean_tags()` 丢掉含这些词的标签后按兜底池补到恰好 5 个；**句末的「。」要留**（第一版清洗脚本把它也当垃圾删了，15 条里 12 条误伤，已还原重洗）。历史条目用 `data/clean_copy_now.py`（CAS 写回；**必须读 `state_json` 原始值判断** —— `BatchStore.get()` 的 `_normalize` 会在内存里先洗一遍）+ `data/sync_publish_copy.py` 就地纠正。`test_batch_copy_prompts_ask_for_creator_voice_not_description` 守住。
- **「等待你的确认」那一屏必须把信息给全，报错必须自带定位信息**（2026-09-13 用户：「报所有可以展示的信息都展示出来」+「是指等待你的确认里的信息」）：审核区在看图/标题/简介/标签之外，新增**只读**的「本条全部信息」（类型 / 状态 / 抖音作品号 / 源作品文案 / 源文件名 / 识别歌曲 / 歌曲情绪 / **造型来源**（沿用源视频 或 按歌曲情绪重做）/ 候选图文件名与版本 / 审核放行 / 条目 id / 下载子任务 / 视频子任务 / 源文件路径 / 创建时间 / 最近更新）。**「条目日志」不要**（用户 2026-09-13 追加：「信息展示 条目日志不要」——`item.logs` 留在后端/接口里排查用，页面不再渲染，`.batch-logs` 相关样式一并删掉）。`BatchRoute` 的 `call()`/`prepare()` 失败时把 `METHOD` 与**具体端点**拼进提示（如「HTTP 500：服务内部错误（NameError）：…　〔POST /api/batches/…/items/…/confirm〕」）。看图 + 确认的流程与按钮不动，仍然没有教学式提示/修改回路。
- **未处理异常一律回 JSON，前端绝不盲目解析响应**（2026-09-13 用户实测「Unexpected token 'I', "Internal S"... is not valid JSON」）：FastAPI 默认把未处理异常回成纯文本 `Internal Server Error`，而前端若干处（App / Migrate / Rvc / UpScale / Lyric 的 `.../latest`、BatchRoute 的批次读写）直接 `response.json()` → 解析错误被当成提示抛给用户，真正的原因与端点完全看不到。现在 `app.py` 有全局 `Exception` 处理器（回 `{"detail":"服务内部错误（类型）：原因","path":…}` + `logger.exception` 记堆栈），`/api/system/stats` 采样失败只降级不 500；前端统一走 `src/api.ts` 的 `readJson`/`readJsonOrNull`（非 2xx 抛「HTTP \<code\>：\<detail\>」，非 JSON 抛「服务返回的不是 JSON」）。`scripts/start-local.ps1` 把后端 stdout/stderr 重定向到 `data/backend-stdout.log` / `backend-stderr.log` —— 之前那次 500 的 traceback 就是因为没重定向而彻底丢了。
- **不许出现「用了 settings 常量却没导入」**（2026-09-13 用户实测：点「音色转换」直接 500「服务内部错误（NameError）：name 'RVC_MODEL' is not defined」——`app.py` 用了 `settings.RVC_MODEL` 却没 import）：这类 NameError 只有真跑到那一行才炸，单元测试与 `tsc` 都拦不住，所以 `test_backend_modules_import_every_settings_constant_they_use` 用 AST 静态扫全 `backend/*.py`。新加常量/新写端点后这个测试必须仍然全绿。
- **批量条目的进度不得造假**（2026-09-14 用户：「批量跳舞视频没有进度吗」）：SCAIL 迁移（跳舞）链路全程**不广播 ComfyUI 采样 progress 事件**，子任务 job 的 `progress` 一直是 `None`；`batch_worker._watch_child` 曾写 `progress=child.get("progress") or 0`，导致条目进度条**整整 35 分钟冻在 0%**（实际已经跑完 7 段、`save` 完成、进了二采），用户以为卡死。现在：① `_watch_child` 保持 `None`（前端 `stepPercent()` 只对真实非零百分比显示数字，0/null 一律按「进度未知」处理，显示进行中脉冲 + `elapsedLabel(step.startedAt)` 的已耗时，随 3.5s 轮询刷新）；② `child_progress_label()` 把条目进度行的说明写成「当前节点 · 分段 X/N · 去字幕 X/N · 二采 X/N」，用真实的分段/批次数字代替假百分比；③ 队列列表里出片中的条目也带「分段 X/Y」。「真实生成流程」面板（子任务里程碑 + 分段/去字幕/二采徽章）依然是唯一的逐节点真相来源。`test_batch_item_progress_never_fakes_a_percentage` 守住这条（含源码级断言：`or 0` 不许被写回来）。**队列里每一条也要显示自己的时间**（2026-09-13 用户：「当前任务队列的时间也给下」）：`BatchRoute.queueTime()` 从条目 `createdAt` 算到 `finishedAt`（或此刻），按状态写成「排队 / 已用 / 耗时」——排队中的条目不许写成「已用」，否则会被误读成已经在跑；只要还有条目在 `running`/`revising`/`confirmed` 就继续每秒跳秒（`useNowTick(batchLive || queueLive)`），批次刚收尾时也不停表。`test_batch_queue_rows_show_their_own_time` 守住。
- **审核点就把「人物图 + 发布文案」写进发布目录**（2026-09-13 用户：「批量创建的时候 `E:\AI_Exports\H3-MotionStudio\发布成品` 这个目录下怎么没有生成对应内容呢」）：以前只有 `_deliver()` 在**成片跑完**后才写三件套，于是条目停在「等待你的确认」时目录里什么都没有 —— 而人物图（用户上传的那张）与发布文案在确认前就已就绪。现在 `batch_worker.deliver_review_materials()` 在 `_prepare_review_work` 落到审核点时、以及 `POST /items/{i}/image`（换图/重写文案后）各调一次：按**同一个** `publish_folder()`（`{三位编号}_{歌名或标题}_{作品号}`，与交付共用）写 `人物图.<原后缀>`（先清掉同名其它后缀，避免换图后留两张）+ `发布文案.txt`，并只把 `image`/`copy`/`folder` 并进 `outputs`——**不碰里程碑、不写 `videoFinal`**，所以「发布文件还没整理 / 重新整理发布文件」的语义不变；成片跑完 `_deliver()` 仍在该目录覆盖补上 `最终成片.mp4`。**页面没有成品面板**（2026-09-13 用户先要去掉「发布文件还没整理」「发布目录已收到人物图与文案」「发布文件已整理」，最后指着一张只剩「最终成片 / 人物图 / 发布文案 / 打开文件夹」的截图追问「这个没有去掉吗 不是说去掉吗」）：`BatchRoute` 里那整块（`batch-output-panel` / `batch-output-grid` / 打开文件夹按钮 / 各 `output/*` 链接）已全部删掉，`openFolder`、`outputsReady`、`hasOutputs` 与不再使用的 `Copy`/`FolderOpen` 图标一并清掉；**交付照做**（文件照样写进发布目录），成片仍可在「生成阶段产物」面板里点开。`test_batch_page_does_not_render_publish_status_chatter` 连这些类名与链接一起守住（剥掉注释后再断言）。**删除/跳过仍然只抢救「子任务 `completed`」的成片**（`_salvage_deliver`），`cancelled` 的子任务没有成片可救。
- **同一条作品已经备过料就直接沿用，不重复建**（2026-09-13 用户：「你可以知道是否已经建立的文件吗，如果已经建立的文件 当我复制抖音链接的时候不要在重复建立了直接往下走」+「我可能只跑完了前面的几步没有让 comfyui 去生成又重新跑了这条任务」）：`batch_store.items_for_aweme()` 按作品号跨批次找历史条目（返回时附 `_batchId` 以便定位目录），`batch_worker._adopt_previous_work()` 在**本条自己还没有 `ai`**（= 没备过料）且**不是用户在要求调整**（`feedback` 为空且 `mode == "both"`）时，取同作品号最近的一条（同类型优先，`updatedAt` 最新）复用：提示词按**本条比例**重拼（与 `set_item_ratio` 同一套构图规格）、候选图 / 取景帧 / 6 帧联系表**复制进本条自己的目录**（绝不跨条目共用同一份文件）、沿用标题/简介/标签与动作运镜或迁移提示词、写 `出图提示词.txt`，状态直接落到 `awaiting_review` 并调 `deliver_review_materials()`，**完全不碰 ComfyUI、不下载、不抽帧、不调模型**。`_prepare_review_work` 在动手做重活**之前**先试这条路。日志会写「这条作品之前已经备过料：直接沿用上次的出图提示词、人物图与发布文案（不会再下载/抽帧/调模型）」。`test_batch_reuses_previous_prep_for_the_same_aweme` 守住这条（含「无产物时不误判」）。
- **一个条目只能有一个发布目录**（2026-09-13 用户：「我只开始了两个任务啊 文件夹多了好多」）：目录名带标题，而标题会变（备料一版、用户上传候选图后 `write_copy` 又改一版），所以 `publish_folder()` **优先复用 `outputs.folder` 记录的目录**，名字过时就 `rename` 过去，rename 失败（目标已存在等）就继续用旧目录，只有记录不在发布根内/已不存在才用理想名字 —— **任何情况下都不为同一条目新建第二个目录**（`_ideal_publish_folder()` 只负责拼名字）。**重新加入队列的同一条作品也不许再建一套**（2026-09-13 用户：「现在加入队列目录又重复生成文件夹了……如果目录里已经有了最终成片说明已经生成过了……再次加入队列时候不要重复生成文件夹」）：`publish_folder()` 现在先扫发布根里 `*_{作品号}` 的既有目录——**优先带 `最终成片.mp4` 的那个**（说明这条出过片）——有就直接复用，只有全新作品才用理想名字（`_existing_publish_folder()`）。历史重复目录用 `data/merge_duplicate_folders.py` + `data/repoint_missing_folders.py` 合并与重指（**注意 `folder` 这个键要直接指向保留目录本身，不能再拼一层旧目录名**）。交付 `_deliver()` 与提前落盘 `deliver_review_materials()` 共用它。历史遗留用 `data/merge_publish_folders.py`（一次性脚本，不进仓库）按作品号合并：以条目引用的目录为保留目录，把缺失文件并进去，同名同大小 / 可再生的 `发布文案.txt` / 更旧的直接丢，只有「更新且不同」才保留该目录并提示人工确认，最后删除已无独有文件的目录。
- **测试绝不许写真实发布目录**（2026-09-13 实测发现）：`test_batch_reuses_previous_prep_for_the_same_aweme` 只换了 `batch_worker.DATA_DIR`、忘了换 `BATCH_OUTPUT_ROOT`，而 `_adopt_previous_work()` 末尾会调 `deliver_review_materials()` 真的落盘——结果在用户的 `E:\AI_Exports\H3-MotionStudio\发布成品` 里留下一个 `001_上一次的标题_<作品号>\` 目录（标题/简介全是测试假数据）。现在 `tests/test_backend.py` 用模块级 `setUpModule()`/`tearDownModule()` 把 `backend.batch_worker.BATCH_OUTPUT_ROOT` 统一钉到临时目录，单个用例要断言目录结构时再自行 `patch.object(batch_worker, "BATCH_OUTPUT_ROOT", root/…)` 覆盖；那个复用用例另外断言 `outputs.image/copy/folder` 必须落在临时根内。凡是要跑 `deliver_review_materials()` / `_deliver()` 的测试都必须走这条。
- **条目标题只有一个来源：`ai.title`**（2026-09-13 用户：「批量生成任务 4 为什么标题不一致」）：`item.title` 是预审阶段写的，而用户上传候选图后 `write_copy` 会**按图重写** `ai.title`（发布文案、发布目录名都用它），旧实现只改后者，于是左侧队列与审核面板/发布会显示两个不同标题。现在 `batch_store._sync_titles()` 在**读取路径**（`_normalize`）把 `item.title` 对齐成 `ai.title`（历史条目读一次就修好），`app.py` 收到上传图时也同步写回；前端 `BatchRoute.itemTitle(item)` 统一取 `ai.title || item.title`，队列行与详情 `<h2>` 都用它。`test_batch_item_title_follows_the_published_title` + `test_batch_queue_and_detail_show_one_title` 守住。
- **抖音下载：本地已有这条作品就直接复用，不再下载**（2026-09-13 用户：「抖音下载的时候如果已经有了就不要下载了」）：下载器子进程自己会跳过已存在的视频（`D:\project\douyin-downloader` 的 `core/video_downloader.py`：「Video %s already downloaded, skipping」→ `result.skipped += 1`，且 `douyin_service.result_for()` 是按 `DOUYIN_OUTPUT.rglob(f"*{aweme_id}*")` 找文件的，所以**跳过也不会报「找不到视频文件」**），但那条路要**先把下载服务拉起来、再提交一次任务**（一个完整 Python 进程 + 一次轮询，占内存、拖慢批量）。因此 `batch_worker.cached_download_path(url)` 在**提交之前**先查本地：① 下载器写的 metadata 清单（`download_manifest.jsonl` 的 `file_paths`）② 没命中就按作品号在下载目录里搜（过滤条件与 `result_for` 一致：视频后缀、排除 `.h3-converted`/`.part.`）；两个根都查（`DOUYIN_OUTPUT` 与 `MANIFEST_PATH.parent`——实测这台机器上 media 在 `E:\AI_Exports\Douyin`，清单在 `D:\EV`，**两边都可能有同名文件，别只认一个**）。命中即由 `_adopt_existing_source()` 复用：照常过 `duplicate_item_by_aweme` 判重、`ensure_download_playable`、写 `sourcePath`/`awemeId`/`sourceMetadata`/`title`、download 里程碑打勾，日志记「本地已有这条视频，跳过下载」，**完全不调用 `douyin_service.submit`**。`test_batch_reuses_an_already_downloaded_source` 守住这条。
- 每个批量条目只生成一份 `发布文案.txt`，内含一套可直接复制的标题、简介和标签，不再分别生成抖音版与 B 站版文案。**不再生成任何封面**（2026-09-10 用户要求「双封面我也会自己生成」）：交付阶段只输出最终成片 + 人物图 + `发布文案.txt`，`render_covers()` 与相关 PIL 绘制函数已删除，`/output` 接口也不再接受 `coverBilibili`/`coverDouyin`。
- 批量条目的画布比例是**条目级**字段（2026-09-10 用户要求「每个视频需要让我选择比例，歌曲默认 4:3、跳舞默认 9:16，我可以改的」）：每条独立保存自己的 `ratio`，默认值由 `settings.BATCH_DEFAULT_RATIOS` / `new_batch_state` 按类型给出（`POST /api/batches` 的 `singingRatio`/`danceRatio` 仍可整组覆盖，属 API 能力、页面不再暴露），`POST /api/batches/{batch}/items/{item}/ratio` 可改；**改动窗口在 2026-09-14 放宽为「只要没开始出片就能改」**（用户：「未开始前的任务都允许修改」）——`batch_worker.item_settings_editable()` 判定，`pending`/`awaiting_review`/`confirmed`/`failed`/`skipped` 都放行，只有 `running`/`revising`/`completed`/`deleted` 拒绝（先点「回到确认」）。比例同时驱动三处：候选人物图的生成档（`batch_image.IMAGE_SIZES` / `batch_portrait`）、出图提示词的构图规格（`batch_ai.compose_image_prompt(ratio=…)`，改比例会按新比例重拼并覆盖 `出图提示词.txt`）、以及出片时提交给 `/api/jobs`（唱歌）或 `/api/jobs/migrate`（跳舞）的 `ratio`。缺 `ratio` 的历史批次按类型默认值兜底（`batch_worker.item_ratio`）。候选图与所选比例不符时只写日志提示，不擅自清空用户自己出的图。
- **「回到确认」与跳舞条目的「去除字幕」开关**（2026-09-14 用户：「我希望可以回到等待你的确认的页面，有可能我需要重新修改内容，现在回不去」+「跳舞视频我想要修改是否去除字幕这个操作」）：`POST /api/batches/{batch}/items/{item}/reopen-review` 把过了审核点的条目退回审核点，`reset_review_row()` 清空 `reviewApproved` 与 `videoJobId`，把 `review` 里程碑恢复成 `running`、`video`/`deliver` 回到 `pending`（页面流程重新变成待办），**旧成片与发布目录不删**（`outputs` 保留，随时还能点开）；条目没在出片时直接退，**正在出片时先按「跳过」同一条链路安全取消子任务**，由 runner 的 `_finish_abandoned()` 看到 `reopenRequested` 再落到审核点（用户明确要求允许这么干），批次若已 completed/cancelled/failed 会一并回到 `awaiting_review`。跳舞条目的「是否去除字幕」以前只由预审模型给出、页面只能看，现在审核区（以及任何还没出片的状态）给了 `POST /api/batches/{batch}/items/{item}/remove-subtitles`（`set_item_remove_subtitles`），规则与画布比例同一条；出片时 `_post_video_job` 按当前值提交 `remove_subtitles`。审核区**仍然只有「看图 + 改这两个开关 + 确认」**，没有重新加回 2026-09-10 删掉的文案反馈/编辑回路。**警告文案必须区分「已放行但还没轮到」和「真的在出片」**（2026-09-13 用户点了一条 `confirmed` 的条目问「我操作的是没有开始的任务为什么回影响到正在生成的内容呢」）：`BatchRoute.renderingNow(item)`＝`status === "running"` 或 `childJob.status ∈ {queued, running, cancelling}`，只有它为真时「回到确认 / 取消出片」才弹「已生成到一半的进度作废」；`confirmed`（已放行、runner 还没轮到）退回确认只是把这次放行作废（后端 `reopen_batch_item_review` 走 `reset_review_row`，不调 `cancel_job`），不碰任何正在跑的生成，也不碰别的条目。同理「还在排队」的提示只在批次真的暂停时才说「点继续后才开始」。
- 批量输入区的「歌曲视频链接 / 跳舞视频链接」各带一个开关（2026-09-10 用户要求「我开了哪个展示哪个，哪个才要执行」）：关掉的一类既不展示输入框，也不会进批次（`start()` 只提交开启的那一类，两类都关时报错）。开关状态与链接一起持久化在页面草稿里。
- 批次随时可以追加任务、单条随时可删（2026-09-10 用户：「可以让我随时添加新的任务，删除单条任务」）：`POST /api/batches/{batch_id}/items`（`batch_worker.append_batch_items`）在批次**跑着 / 暂停 / 等审核 / 已完成 / 失败**时都能把新链接排到队尾，编号接着往下排，`ratio` 按类型默认值给；同批上限仍是 `MAX_BATCH_ITEMS`=50；只有 `cancelled` 批次不能复活（需新建）。追加只排队、**不会自己跑起来**（走 `_wake_batch()`，见下一条）。页面上输入区不再因为「批次未结束」而禁用，按钮是「加入队列（N 条）」，提交成功后清空对应链接框；单条删除按钮是「删除这一条」并带二次确认（运行中的条目会先安全取消子任务）。
- **出片在后台跑，runner 同时继续给后面的条目备料**（2026-09-13 用户实测：「我现在重新加入了一条…下载抖音视频、生成人物图与发布文案、等待你的确认…现在没有处理啊」）：`run_batch` 不再原地 `await _process_confirmed`，而是把它丢进后台任务（强引用集合 `_VIDEO_TASKS`），自己继续循环备料；出片本身仍**严格一条一条**（`_next_work(state, allow_confirmed=video_task is None)` 在后台出片时不返回新的 `confirmed`）。备料只用下载 + ffmpeg 抽帧 + 一次文本模型调用，不碰 ComfyUI，所以能和出片并行；暂停仍不打断正在出片的这一条（等它落定再暂停），失败/取消复用 `_fail_item_and_batch` / `_finish_abandoned` 同一套收尾。防呆：出片任务正常返回却没把条目推进出 `confirmed` 时直接判失败停下（否则会无限重投出片任务，该分支由复现脚本实测出来过）。
- **`scripts/batch_prep_item.py`**：不重启后端、单独给某个排队中的条目备料（`--batch <id> --index 3` 或 `--item <id>`）。它把 `batch_worker.batch_store` 换成 CAS 版本（`UPDATE … WHERE state_json = 我读到的那一份`，冲突就重读重算）后复用 `_download` + `_prepare_review`，因此不会和 runner 每 ~3 秒的整份状态写回互相覆盖（普通 `BatchStore.update` 是进程内锁，跨进程会互相吃掉写入，比如用户刚点的「跳过」）。用完会把停在 `completed` 的批次拉回 `awaiting_review`。
- 重复任务必须过滤掉（2026-09-10 用户：「重复的记得过滤掉」），两层判重都带**类型**（同一段素材当歌曲和当跳舞是两件事，不能互相吃掉）：① 入队时按 `batch_worker.item_key()`＝类型 + `url_key()` 比对已有条目，命中则跳过并把条数写进 notice（同一次粘贴内部的重复也算进这个计数）；只有「已删除 / 已跳过」不参与判重，用户明确不要的可以再加回来；一条都没新增时页面必须弹黄色中性提示（`batch-alert info`），不能只在 notice 里悄悄改一行字。② 下载拿到 `awemeId` 后再兜一次 `batch_worker.duplicate_item_by_aweme()`——短链与 `www.douyin.com/video/{id}` 是两种写法、链接指纹拦不住，只有作品号能认出同一条；命中抛 `DuplicateItem`，由 `run_batch` 标成 `skipped` + `warning`（**不是 failed**，也不拦住后面的条目），且不再做转码/预审/出片。
- `url_key()` **只丢跟踪参数**（`from_tab_name`/`showTab`/`msToken`/`utm_*`/`share_*`…），有意义的参数一律保留：抖音「喜欢列表」链接是 `www.douyin.com/user/self?from_tab_name=main&modal_id=<作品号>&showTab=like`，**作品号在 query 里**；2026-09-11 用户实测「点击了准备任务怎么没有效果」就是早先实现把整段 query 丢掉，所有这类链接归一成 `www.douyin.com/user/self`，两条不同视频被当成重复过滤，点了没有任何新增。改这里必须同时满足：不同 `modal_id` 得到不同 key、同一链接带不同跟踪参数得到相同 key（`test_duplicate_links_and_works_are_filtered_out` 覆盖）。
- 手动关闭 ComfyUI（2026-09-10 用户：「关闭启动的 comfyui」）：`POST /api/comfy/stop` → `pipeline.ResourceManager.shutdown_comfy(job_id=None)`（先 `POST /comfy/free` 卸载模型，再 terminate/kill 自启进程、或按 8188 监听进程 `taskkill /T /F`；只认 `COMFY_PYTHON`/`d:\\comfyui\\main.py`，其它进程拒绝关闭）。关闭后确认端口真的释放，**第一次没杀干净会自动补一刀重试**（实测偶发只终止包装进程）。队列面板（`QueuePanel.tsx`）在「ComfyUI 已连接」时显示「关闭 ComfyUI」按钮，两次点击确认。有任务在跑时接口直接 409 拒绝：jobs 表有 queued/running、批次有 running/revising/confirmed、或 ComfyUI 队列里还有 prompt 一律不关，避免把正在生成的图/视频打断。链路内的 `resources.stop_comfy(job_id)` 保留原语义（推进 handoff 里程碑后调用同一个 `shutdown_comfy`）。
- 歌曲条目的「人物动作要求 / 运镜要求」（跳舞条目则是内容/视频人物/参考图人物三段迁移提示词）由预审那一次模型调用产出（`ai.action_prompt`/`ai.camera_prompt`/`ai.content_prompt`/…），模型降级时在**预审阶段**就用 `default_action_plan(duration)` 按时长铺开兜底（不只是提交时才兜），出片时按原文提交给工作流。这些提示词在条目详情里**只读展示**给用户核对（2026-09-10 用户要求「有填写要展示出来给我看」），不提供编辑入口。
- 画布比例在批量页面的入口**不在输入区**（2026-09-10 用户：「不要展示在下边会引起误会，直接在审核时展示比例就好了」）：输入区不做分组比例选择（默认值由后端 `BATCH_DEFAULT_RATIOS` 给，歌曲 4:3、跳舞 9:16，链接框下只留一行文字说明默认值），条目头部也没有独立比例条、条目列表不带比例角标。**2026-09-14 起条目详情里有两处入口，同一时间只会出现一处**：`awaiting_review` 在审核区的文案字段里给「画布比例 / 去除字幕」两行（`renderSettings`）；其它**还没开始出片**的状态（pending / confirmed / failed / skipped，且已备过料）在「出片前设置」面板里给同样的两行。`POST /api/batches` 仍接受 `singingRatio`/`danceRatio` 作为整组默认值（API 能力保留）。

- 人物定妆是 `/portrait` 路由：4:3 固定对应唱歌胸像近景并送入歌曲生成，9:16 固定对应跳舞人物并送入动作迁移；造型来源由用户每次选择视频取帧、上传参考图或不使用参考图。必须先分析背景/服装等方案并取得明确确认，关键输入变化后确认自动失效，禁止未确认直接生成。
- 人物定妆唯一身份原型固定为 `E:\AI_Assets\PortraitIdentity\本人固定参考.png`（源文件 `1526e64b-274a-497b-8451-5baa5f554de6.png`）；`4x3唱歌构图参考.png`（源 `ca8aec93-96d5-4df7-9960-f881ae51c811.png`）和 `9x16跳舞构图参考.png`（源 `87b12a2a-c696-4d98-b3f2-42758170bd7b.png`）仅定义构图范例，不是身份图、也不强制作为每次造型输入。
- 人物定妆生成结果写入 `E:\AI_Exports\PortraitStudio\4x3` 或 `E:\AI_Exports\PortraitStudio\9x16`；OpenAI 凭据只从进程环境读取，严禁写入前端、数据库或仓库。
- The selected visual source is `design/reference-ui.png`.
- The global shell and all route styling must follow `design/douzy-shell-reference.png`: fixed left navigation, deep indigo canvas, cyan active accents, low-contrast violet borders, restrained density, and generous empty space.
- The H3 generation workspace remains the `/` route; Douyin download is a separate `/douyin` route inside the same shell.
- This is a local desktop-style workspace, not a marketing site.
- The only editable generation inputs are one character reference image, one singing video, character action instructions, and camera instructions.
- Progress must reflect real ComfyUI nodes and RVC subprocess stages, including exact errors.
- The pipeline is strictly single-chain: ComfyUI generation and RealESRGAN finish first, ComfyUI then closes completely, RVC converts the voice, and the converted audio is muxed into the final MP4.
- The 1080P second pass must use the ComfyUI workflow `视频-成片输入-独立二采-RealESRGAN4x转1080P-8GB高清加强版.json` and must wait for every VHS meta-batch requeue to finish before ComfyUI is closed.
- The completed state must display the final video and preserve access to the original video.
- **项目内 RVC 转换脚本**（`D:\Comfyui\RVC\convert_video_to_my_voice.py`，由 `settings.RVC_SCRIPT` 引用、歌曲/迁移/RVC 链路使用）与 H3 影动高清工作台的默认 RVC 音色统一为 **`mogoo_v1`**（v1 / 40k / RMVPE，**200 epoch / 62,400 步**，用抖音博主「Mogoo蘑菇」主页全部 198 个作品的 193 条干声 / 75.0 分钟 / 1248 片段训练；发布态取自 `mogoo_v1_e200_s62400.pth`，sha256_16=`4757c841c24dacd8`；素材与再训流程归档在 `E:\AI_Assets\RVC\Projects\mogoo_v1\`），使用 `D:\Comfyui\RVC\assets\weights\mogoo_v1.pth` 与 `assets\indices\mogoo_v1.index`（2026-09-11 用户要求，替换 wanwansu_v1）。前端 `src/App.tsx` 与 `src/RvcRoute.tsx` 的 `DEFAULT_VOICE_LABEL` 必须同步。该脚本自身写死的 `MODEL_NAME`/`INDEX_PATH` 默认值同样是 mogoo_v1（脚本在仓库外、不受版本控制，改完要单独确认）。便携音色转换器（`D:\soft\MyVoiceConverter\SasablueVoiceConverter`）同步切到同一个音色。上一版 wanwansu_v1 的模型与数据集保留归档，不再作为默认。
- **RVC 轮数不等于质量，按实测择优**（2026-09-11 用户问「不需要训练 200 次吗」后确立，并被数据纠正过一次）：RVC 的 epoch = 把 filelist 走一遍，数据量涨 10 倍后一轮的代价也涨 10 倍，**轮数不能横向比**；比较基准是总梯度步数（mogoo_v1 的 200 轮 = 62,400 步；旧 wanwansu_v1 的 200 轮仅 6,200 步）。**关键教训：损失进入平台 ≠ 下游音色指标不再改善。** mogoo_v1 前 48 轮时 mel 损失已在 22.3~24.6 震荡无下降趋势，据此判断「已收敛、不必加练」——但 CAM++ 端到端实测 5 个检查点后，指标随训练**单调改善**（e40→e200：外人位移均值 +0.1445→+0.1698，本人侧边距 +0.0053→+0.0233，e40 在两项上都是最差），最终发布的是补训到 200 轮的 e200。所以加练与否则必须**用下游指标测**：`E:\AI_Assets\RVC\Projects\mogoo_v1\scripts\eval_candidates_convert.py` + `eval_candidates_score.py`，测试集至少 4 条外人干声 + 2 条本人干声（单条会选歪），并按**事先写死的规则**择优（先要求本人素材仍落在本人这一侧，不合格直接淘汰；再取外人位移均值最高者；差距 ≤0.01 算平手、判给更晚的检查点），不要凭「更多轮更好」或「已平台期」任一侧的直觉下结论。`train.py` **默认从 `logs/{exp}/G_*.pth` 断点续训**（`epoch_str` 取自检查点，`-te` 给目标总轮数即可），不会从头开始；`-l 1` 只覆盖 `G_2333333.pth` 作续训检查点，`-sw 1` 才在每个存档点写可发布的小模型，而**训练结束时会自动把最终小模型写成 `assets/weights/{exp}.pth`**——发布脚本若用错源文件覆盖它，那个成品会永久丢失（2026-09-11 就这样丢过 e48 成品）；另外**存档点只有 epoch 是 save_every 整数倍时才有小模型**（48 不是 10 的倍数，所以当时的 48 轮成品只能靠训练结束那次自动保存）。
- **注意别搞混**：桌面快捷方式「便携音色转换器」指向的是**另一个独立工具** `D:\soft\MyVoiceConverter\SasablueVoiceConverter\`（7866 端口的网页 GUI），**不属于本仓库、项目零引用**，它有自己的 `configs\config.json` 与 `assets\weights`。改音色时两边都要确认，不要以为改了项目脚本就等于改了它。
- Douyin downloads save to `D:\EV` by default (env `H3_DOUYIN_OUTPUT` overrides). The H3 backend starts the downloader service with `DOUYIN_PATH` set to the same directory so both sides agree.
- The on-demand Douyin downloader REST service defaults to `127.0.0.1:9100` because this Windows host reserves TCP ports 8948–9047 and rejects the old port 9000 with WinError 10013; `H3_DOUYIN_DOWNLOADER_URL` still overrides it.
- The Douyin downloader service (separate Python process) must be memory-lean for ComfyUI: start it only on explicit user actions (submitting a download / login window), never from page reads; stop it automatically after 60 s of download inactivity (`IDLE_STOP_SECONDS`). An on-disk job mirror (`data/douyin-jobs.json`) keeps finished jobs listed and playable while the service is offline.
- The upload-card singing-video preview must survive HEVC: picking a file stores it via `/api/uploads/preview` and serves an H.264 copy when the codec isn't browser-safe (`data/uploads/`, 24 h cleanup); the pipeline still receives the original file unchanged at submit.
- Every completed Douyin download must be converted in place to a browser-playable H.264/AAC MP4; after a successful atomic replacement, no HEVC original or duplicate source file is retained.
- Closing or refreshing the workspace must preserve the latest character image, singing video, action/camera instructions, and job state; reopening the page must restore the draft and resubscribe to persisted backend progress until the job ends.
- The action-migration workspace is the `/migrate` route inside the same shell (sidebar third entry 动作迁移). Page order: canvas ratio first (default 9:16 竖版) → upload action video (required) + optional portrait reference (any aspect; fallback `singing_portrait_4x3_1440x1080.png`) → migration mode 动作迁移 (default) / 人物替换 → prompts (content/video person/image person) → toggles 去除字幕 (default off) and 二采放大 4× (default on).
- The migrate chain runs the workflow pair in one strict job: optional 视频-去字幕-ProPainter-固定底部 (its output is copied into ComfyUI input and becomes the drive video) → 视频-长视频替换-4x3加速版-ProPainter输入 (#563 drive, #30 reference, #353 false=动作迁移/true=人物替换, #545/#509/#510 prompts, #456 output) → optional RealESRGAN 成片二采 pass (default on). Output keeps the source audio and frame rate; no RVC on this route; single-job mutual exclusion with the singing route.
- 去字幕（ProPainter 512×896 单次常驻整段帧+双遮罩 ≈9.2MB/帧、叠加 RAFT 相关体积尖峰 ≈2.3GB）在 8GB 显存上长视频会 OOM，因此按帧分批执行（8GB 显存保护）：源帧数超过 `CANVAS_PARAMS[比例].clean_batch_frames`（9:16=300 帧/批、4:3=900 帧/批；段间重叠 `clean_batch_overlap`=24 帧提供边界时序上下文）时，pipeline 用 VHS 帧窗口（skip_first_frames/frame_load_cap）逐批跑同一去字幕工作流，逐段按帧裁剪重叠后 concat、回灌原音频，拼接帧数必须与源帧数一致；超限判定/拆段参数都在 `backend/settings.py`，不碰工作流 JSON。执行面板右上显示「去字幕 X/N」徽章（样式同分段徽章），paint 里程碑进度条由后端按批内耗时/预估时长平滑投影（该工作流无采样节点、ComfyUI 不广播 progress 事件）；进度状态字段 `cleanBatch`/`cleanBatches`/`cleanBatchEstSecs` 在 cleaning 阶段写入、结束后清空。
- ProPainter 内部 RAFT 会把每 12 帧（11 对相邻帧）打包成一个 batch 计算全分辨率相关体积（512×896 下 ≈2.3GB 单次分配，8GB 卡上易 OOM）：本地补丁把帧对按每批 ≤6 对拆分推理再拼接（光流逐对独立，结果逐位一致、只降峰值），补丁文件 `D:\Comfyui\ComfyUI\custom_nodes\ComfyUI_ProPainter_Nodes\model\modules\flow_comp_raft.py`（ComfyUI 环境，不在本仓库版本控制内）。
- 去字幕遮罩位置自动检测：固定"贴底"遮罩对竖屏歌词字幕（多在画面 0.75–0.85H、非贴底）会落空，因此提交去字幕工作流前由 `backend/subtitle_detect.py` 自动定位持续字幕条——只取视频 30%~97% 时段抽帧（≤1280 宽等宽缩放、跳过片头标题），逐帧算行边缘能量的「相对 ±110px 邻域尖峰比 ≥1.45」得候选带，跨帧投票（≥40% 采样帧出现、带宽 10–260px），带内列覆盖 p1~p99（阈值放宽到 mean+0.6σ）定横向范围并外扩（单侧 ≥ 带宽 12% 且 ≥20 源像素、两侧留 ≤3% 画布安全边，避免两端淡笔画残留）；检测结果按 VHS 拉伸映射（画布坐标 = 源坐标 × 画布/源尺寸）换算后覆盖 `CANVAS_PARAMS` 的 mask 参数（`mask_params_from_box`），检测失败/关闭时回退固定底部。检测器用 ComfyUI 自带 python（`COMFY_PYTHON`，含 numpy/Pillow）执行；环境变量 `H3_SUBTITLE_DETECT=0` 关闭自动检测。
- Canvas parameter groups live in `backend/settings.py` `CANVAS_PARAMS` (4:3 = 512×384, mask 430×58 at (256,344), HD 1440×1080; 9:16 = 512×896 per SCAIL2-Easy 512p rule, mask 430×135 at (256,803), HD 1080×1920). Tune numbers there, never in the JSONs.
- 歌曲生成路由同样先选画布比例（默认 4:3，可选 9:16 竖版，全链路按此输出）：参数组在 `backend/settings.py` `SINGING_CANVAS_PARAMS`（4:3 = 640×480、参考图缩放档 0.31MP；9:16 = 480×864、0.41MP），运行时替换唱歌工作流五个 H3 分段节点 15/29/400/420/440 的宽高与节点 269 的缩放档。分段构图提示词的比例文案由 ComfyUI 歌词节点 `H3AutoLyricsFromAudio5StyleSafeCamera` 的 `canvas_ratio` 输入控制（4:3 为节点默认、逐字节不变；9:16 改写为竖版 480×864 文案）；该补丁位于 `D:\Comfyui\ComfyUI\custom_nodes\h3_media_duration_router\__init__.py`（ComfyUI 环境，被其仓库 .gitignore 排除、不在本仓库版本控制内）——旧版节点缺少该输入时，9:16 唱歌任务会在提交 ComfyUI 前明确报错提示重启，4:3 不受影响。
- 歌曲生成运行中的「H3 分段 X/N」徽章（执行流程面板右上，样式与动作迁移分段徽章一致）：预估段数在任务创建时按源时长写入 `estimatedSegments`（`backend/pipeline.py` `estimate_singing_segments`：第 1 段 362 帧 @24fps ≈15.08s、每增一段多 340 帧，段间 22 帧续接，容量 362/702/1042/1382/1722，上限 5 段；边界常数在 `backend/settings.py` `H3_FPS`/`H3_CLIP_FRAMES`/`H3_CONTEXT_FRAMES`）；运行时按 CLIP 锚点节点推进 `currentSegment`（15/18→段1，29/32→段2，400/403→段3，420/423→段4，440/443→段5；H3 懒加载只跑所需前 N 段、节点只执行一次且按段顺序，段位只推进不回退）。
- Backend code changes only take effect after the local uvicorn service restarts and `npm run build` refreshes `dist`; while the user has a job running, do not restart the service or rebuild the frontend. **重启前必须机械地检查一次，不能凭印象**：`GET /api/comfy/queue` 的 `app.status` 不在 `{null, completed, failed, cancelled}`，或 `jobs` 表里有 `status='running'`，或批次里有条目处于 `running`/`confirmed`（即已点确认、正在出片），就**一律不许重启**，等它跑完再重启。2026-09-10 因为没查就重启，把用户刚确认的 H3 出片任务打断成 `interrupted`，批次被标 failed。
- After each completed implementation batch, commit and push the changes to `origin/main`.
- The final deliverable of an independent upscale job is stored on disk under the marked name `{job_id}_upscale_最终版.mp4` (the workflow's audio-muxed `*_00001-audio.mp4` is atomically renamed and the job state updated at job end); the same-prefix `*_00001.png` (first-frame preview) and the audio-less `*_00001.mp4` are intermediates. The upscale result panel and history picker always point at the marked final file only.
- 二采放大（`/upscale`）的放大倍数**固定 4×**（`RealESRGAN_x4plus`）：页面不再提供 2× 选项，`POST /api/jobs/upscale` 也不再接受 `multiplier` 表单字段（后端直接写 `multiplier="4x"`、`upscaleModel=UPSCALE_MODEL_X4`）。结果面板只按任务状态里的倍数显示，历史 2× 任务仍标注 2×。
- 歌曲生成（`/`）与动作迁移（`/migrate`）各自带一个**默认开启的二采放大开关**（`use_upscale` 表单字段 → `state.useUpscale`）：歌曲生成链路是 H3 原版 → 二采 4× 收 1080p（`enhancedOutput`）→ 关闭 ComfyUI → RVC → 最终版，二采必须在 RVC 之前、ComfyUI 关闭之前完成；动作迁移链路是迁移成片 → 二采 4× → `mark_final_version` 标记的最终版。共用 `pipeline.run_upscale_pass`（写入 `upscaleBatch`/`upscaleBatches` 分批进度，不占用 H3/迁移分段徽章的 `currentSegment`/`estimatedSegments`）；关闭开关则成片保持生成分辨率。前端 `App.tsx`/`MigrateRoute.tsx` 用 `useUpscale` 控制里程碑显隐与草稿恢复，执行面板显示「二采 X/N」徽章。歌词字幕开关**不加**（用户明确取消）。
- 成片入口只有「最终成片」与「原版成片」两个（`backend/app.py` `OUTPUT_MEDIA_FIELDS`）：去字幕视频/迁移成片/高清成片等中间产物仍写状态与磁盘，但不再出现在结果区与 `/api/jobs/recent`；`MEDIA_FIELD_BY_KEY` 保留全部键供 `source_key` 解析兼容历史任务。
- 独立音色转换是 `/rvc` 路由（sidebar「创作与管理」第四项，位于二采放大之下、歌词字幕之上）：上传本地视频或从最近任务选「原版成片/最终成片」→ `POST /api/jobs/rvc`（kind=`rvc`）→ `resources.stop_comfy` 先彻底关闭 ComfyUI（handoff 里程碑由它推进）→ `run_rvc`（Demucs 分离 + kikiV1 转换（项目内脚本），`[1/5]`~`[5/5]` 驱动 stems/voice/mux 里程碑）→ `mark_final_version` 标记 `{job_id}_最终版.mp4`。**歌曲生成链路的音色转换必须基于源视频的原唱音轨**（`pipeline.video_with_source_audio()` 把成片视频轨与 `state["sourcePath"]` 的音轨合成后再转换），不是 H3 生成的音频；独立 `/rvc` 路由没有单独的源视频，仍用待转换视频自己的音频。里程碑模板 `store.rvc_milestones()`，执行体 `pipeline.run_rvc_job`；无音轨的源在启动转换前明确报错；与其它任务共用 `pipeline_lock` 单任务互斥。`source_key=rvc` 的任务不出现在该路由的「从最近任务选」列表里（避免自我循环转换）。
- 歌词字幕是 `/lyrics` 路由（sidebar 第五项；**2026-09-10 起已从侧边栏隐藏并关掉前端入口**，理由用户原话「效果太差」，但 `LyricRoute.tsx`、`/api/lyrics/*`、`POST /api/jobs/lyrics`、`pipeline.run_lyrics_job` 全部保留以便恢复；**批量流程里的这一步也已在 2026-09-14 去掉**）。要点：网易云搜歌取词（过滤和声/制作人/混音等制作人员行）→ 用户确认歌词行 → `scripts/lyrics_stage.py`（RVC venv，HDEMUCS 人声 + faster-whisper，默认 `LYRICS_ASR_MODEL`=D:\tmp\fw-small，注入网易云歌词作 initial_prompt，空结果自动重建重试 ≤3 次，仍空则明确报错 **禁止等距铺假字幕**）→ 中文时间轴必须走 `scripts/lyrics_force_align.py`（FunASR `fa-zh` 逐字时间戳，`LYRICS_ALIGN_PY`/`LYRICS_ALIGN_MODEL` 可覆盖，失败即终止任务，不许静默用旧时间轴），非中文保留 whisper 词时间戳兼容路径 → 烧录 mp4（剪映手书 JYgangbi，fontSize≈88×H/1080；双语=原词上/中文下，同刻两条先写翻译行占底再叠原词行；**ASS 时间必须 `h:mm:ss.cc` 两位百分秒**）。`backend/lyrics_worker.py` 的 `align_line_times` 用 difflib 找强窗口锚点（≥0.72 相似度、**比较前用 `zhconv` 统一转简体**、剔除偏移偏离共识簇 >6s 的假锚点、按实测时间排序后再做 |Δ|<0.45s 去重、越界尾行截除），未命中行按官方时间映射且只允许落在锚点区间内。Srt 辅助文件写 `data/lyrics/{job_id}/`、**不与成片同目录**（否则播放器字幕会和烧录字幕叠成两行）。里程碑 read→stems→asr→align→render，不经过 ComfyUI/RVC，但与其它任务共用 `pipeline_lock`。
- 中文歌词最终时间轴必须走成熟的强制对齐方案：`scripts/lyrics_stage.py --vocals-out` 持久化 HDEMUCS 人声，Whisper/difflib 只筛出片段实际唱到的歌词行；随后 `scripts/lyrics_force_align.py` 用 FunASR 官方 `fa-zh` 时间戳预测模型输入「分离后人声 + 已确认歌词」取得逐字时间并还原逐行起止点，禁止再用歌词库时间或手写插值作为中文成片最终时间轴。运行环境默认 `LYRICS_ALIGN_PY=D:\Comfyui\FunASR\.venv\Scripts\python.exe`、模型 `LYRICS_ALIGN_MODEL=D:\tmp\funasr-fa-zh`，可用 `H3_LYRICS_ALIGN_PY` / `H3_LYRICS_ALIGN_MODEL` 覆盖；模型失败必须明确终止任务，不能静默生成旧时间轴。非中文暂保留 whisper 词时间戳兼容路径。
- 里程碑：read→stems(Demucs)→asr(自动语种识别)→align→render；全程不经过 ComfyUI/RVC，但与其它任务共用 `pipeline_lock` 单任务互斥；`originalReady` 指源视频（data/lyrics/{job}/source.*），最终成片在 `output/video/H3_MotionStudio/{job_id}_歌词字幕.mp4`。

## Git rule（用户硬性规则：每次修改完成必须提交 GitHub）

- 每一次修改（不论大小：前端、后端、样式、文案、文档、配置、工作流参数）完成后，只要已验证通过，就必须立即 `git add` → `git commit` → `git push origin main`，并在同一次回复中告知用户提交哈希。
- 不允许在一个回合结束时留下已完成但未提交的改动；提交动作不得拖延到"批量攒齐"再执行。
